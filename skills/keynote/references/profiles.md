# Profiles

The base deck profiles, as numbers the build and the arc checks read. Swappable — a different profile set is an edit here, not a skill rewrite.

> Stub. The three profiles (`fast`, `curated-wow`, `statement`) and their step budgets land with the author mode.
