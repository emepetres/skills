# Speech rates

Per-language words-per-minute defaults the rehearse mode reads to turn a deck into a timing estimate. Swappable per speaker.

> Stub. The `en` / `es` calibration lands with the rehearse mode.
